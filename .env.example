# RabbitMQ Configuration
# ====================
# You can set individual components or use the full URL

# Individual components (will be used to build RABBITMQ_URL if not set)
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest
RABBITMQ_VHOST=%2f

# OR use the full URL directly (overrides individual components)
# RABBITMQ_URL=amqp://guest:guest@localhost:5672/%2f

# HTTP Server Configuration
# ========================
SERVER_HOST=127.0.0.1
SERVER_PORT=8081

# Docker Compose Configuration
# ============================
# These are used by docker-compose.yml
DOCKER_RABBITMQ_USER=guest
DOCKER_RABBITMQ_PASSWORD=guest
DOCKER_RABBITMQ_HOST=rabbitmq
DOCKER_RABBITMQ_PORT=5672

# Environment
# ===========
# You can set this to 'development', 'production', etc.
# ENVIRONMENT=development
